--- a/src/lib.rs
+++ b/src/lib.rs
@@ -65,6 +65,8 @@ pub mod buffer;
 
 #[cfg(feature = "conn")]
 pub mod conn;
+#[cfg(feature = "conn")]
+pub use crate::conn::UdpSendOptions; // Added for wifi-verify: per-packet UDP options
 
 #[cfg(feature = "ifaces")]
 pub mod ifaces;
